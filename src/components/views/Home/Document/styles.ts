// Packages:
import styled from 'styled-components';


// Styles:
import {
  AssetWrapper,
  Asset,
  AssetIcon,
  AssetName,
  AssetGlanceDetails,
} from '../../../../styles';

export const Wrapper = styled(AssetWrapper)``;

export const DocumentWrapper = styled(Asset)``;

export const DocumentIcon = styled(AssetIcon)``;

export const DocumentName = styled(AssetName)``;

export const DocumentGlanceDetails = styled(AssetGlanceDetails)``;
