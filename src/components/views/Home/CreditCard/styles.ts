// Packages:
import styled from 'styled-components';


// Styles:
import {
  AssetWrapper,
  Asset,
  AssetIcon,
  AssetName,
  AssetGlanceDetails,
} from '../../../../styles';

export const Wrapper = styled(AssetWrapper)``;

export const CreditCardWrapper = styled(Asset)``;

export const CreditCardIcon = styled(AssetIcon)``;

export const CreditCardName = styled(AssetName)``;

export const CreditCardGlanceDetails = styled(AssetGlanceDetails)``;
