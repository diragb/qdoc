// Packages:
import styled from 'styled-components';


// Styles:
import {
  AssetWrapper,
  Asset,
  AssetIcon,
  AssetName,
  AssetGlanceDetails,
} from '../../../../styles';

export const Wrapper = styled(AssetWrapper)``;

export const FolderWrapper = styled(Asset)``;

export const FolderIcon = styled(AssetIcon)``;

export const FolderName = styled(AssetName)``;

export const FolderGlanceDetails = styled(AssetGlanceDetails)``;
