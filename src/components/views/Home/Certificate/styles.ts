// Packages:
import styled from 'styled-components';


// Styles:
import {
  AssetWrapper,
  Asset,
  AssetIcon,
  AssetName,
  AssetGlanceDetails,
} from '../../../../styles';

export const Wrapper = styled(AssetWrapper)``;

export const CertificateWrapper = styled(Asset)``;

export const CertificateIcon = styled(AssetIcon)``;

export const CertificateName = styled(AssetName)``;

export const CertificateGlanceDetails = styled(AssetGlanceDetails)``;
